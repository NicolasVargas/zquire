{
  "name": "zquire",
  "version": "0.4.0",
  "description": "Enables modules requirement and their runtime installation if not found. Modules are returned through a callbacks or promises.",
  "keywords": [
    "require",
    "npm",
    "install",
    "runtime",
    "callback",
    "promise"
  ],
  "dependencies": {
    "debug": "^2.2.0",
    "es6-promise": "^2.3.0",
    "introspect-fun": "^1.0.0",
    "npm": "^2.12.1"
  },
  "scripts": {
    "lint": "eslint --ext .js ./lib ./test",
    "test": "istanbul cover node_modules/mocha/bin/_mocha -- -u exports -R spec ./test/",
    "pretest": "npm run lint"
  },
  "devDependencies": {
    "chai": "^2.3.0",
    "eslint": "^0.24.1",
    "istanbul": "^0.3.14",
    "lodash": "^3.10.0",
    "mocha": "^2.2.5",
    "node-uuid": "^1.4.3",
    "require-dir": "^0.3.0"
  },
  "license": "MIT",
  "author": "Nicolas Vargas"
}
